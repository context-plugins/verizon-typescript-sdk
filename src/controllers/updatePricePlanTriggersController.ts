/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  UpdateTriggerRulesBody,
  updateTriggerRulesBodySchema,
} from '../models/containers/updateTriggerRulesBody.js';
import {
  TriggerResponse,
  triggerResponseSchema,
} from '../models/triggerResponse.js';
import { BaseController } from './baseController.js';
import { RuleRestErrorResponseError } from '../errors/ruleRestErrorResponseError.js';

export class UpdatePricePlanTriggersController extends BaseController {
  /**
   * Updates a usage trigger at the account level, device level or a price plan trigger for all devices
   * on the account
   *
   * @param body         Update a trigger
   * @return Response from the API call
   */
  async updateTriggerRules(
    body: UpdateTriggerRulesBody,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<TriggerResponse>> {
    const req = this.createRequest('PUT', '/v2/triggers');
    const mapped = req.prepareArgs({
      body: [body, updateTriggerRulesBodySchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.defaultToError(RuleRestErrorResponseError, 'Error response');
    req.authenticate([{ thingspaceOauth1: true }, { vZM2mToken: true }]);
    return req.callAsJson(triggerResponseSchema, requestOptions);
  }
}

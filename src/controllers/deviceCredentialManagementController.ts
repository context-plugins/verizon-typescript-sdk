/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  CredentialsRequest,
  credentialsRequestSchema,
} from '../models/credentialsRequest.js';
import { DropResponse, dropResponseSchema } from '../models/dropResponse.js';
import {
  GenerateResponse,
  generateResponseSchema,
} from '../models/generateResponse.js';
import {
  RetrieveResponse,
  retrieveResponseSchema,
} from '../models/retrieveResponse.js';
import { BaseController } from './baseController.js';
import { ApiError } from '@apimatic/core';
import { ErrorResponseError } from '../errors/errorResponseError.js';

export class DeviceCredentialManagementController extends BaseController {
  /**
   * @param body
   * @return Response from the API call
   */
  async retrieveCredentials(
    body: CredentialsRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<RetrieveResponse>> {
    const req = this.createRequest('POST', '/credentials/retrieve');
    req.baseUrl('Hyper Precise Credentials');
    const mapped = req.prepareArgs({ body: [body, credentialsRequestSchema] });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.throwOn(400, ErrorResponseError, 'Bad Request / Verification Failure');
    req.throwOn(401, ApiError, 'Unauthorized');
    req.authenticate([{ thingspaceOauth: true, vZM2mToken: true }]);
    return req.callAsJson(retrieveResponseSchema, requestOptions);
  }

  /**
   * @param body
   * @return Response from the API call
   */
  async generateCredentials(
    body: CredentialsRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<GenerateResponse>> {
    const req = this.createRequest('POST', '/credentials/generate');
    req.baseUrl('Hyper Precise Credentials');
    const mapped = req.prepareArgs({ body: [body, credentialsRequestSchema] });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.throwOn(400, ErrorResponseError, 'Bad Request');
    req.authenticate([{ thingspaceOauth: true, vZM2mToken: true }]);
    return req.callAsJson(generateResponseSchema, requestOptions);
  }

  /**
   * @param body
   * @return Response from the API call
   */
  async resetCredentials(
    body: CredentialsRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<GenerateResponse>> {
    const req = this.createRequest('POST', '/credentials/reset');
    req.baseUrl('Hyper Precise Credentials');
    const mapped = req.prepareArgs({ body: [body, credentialsRequestSchema] });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.throwOn(400, ErrorResponseError, 'Bad Request');
    req.authenticate([{ thingspaceOauth: true, vZM2mToken: true }]);
    return req.callAsJson(generateResponseSchema, requestOptions);
  }

  /**
   * @param body
   * @return Response from the API call
   */
  async dropCredentials(
    body: CredentialsRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<DropResponse>> {
    const req = this.createRequest('POST', '/credentials/drop');
    req.baseUrl('Hyper Precise Credentials');
    const mapped = req.prepareArgs({ body: [body, credentialsRequestSchema] });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.throwOn(400, ErrorResponseError, 'Bad Request');
    req.authenticate([{ thingspaceOauth: true, vZM2mToken: true }]);
    return req.callAsJson(dropResponseSchema, requestOptions);
  }
}

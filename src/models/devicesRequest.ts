/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, object, optional, Schema, string } from '../schema.js';
import {
  DevicesRequestFilter,
  devicesRequestFilterSchema,
} from './containers/devicesRequestFilter.js';

/** Request body for retrieving devices based on vendorID and optional filters */
export interface DevicesRequest {
  /** The ID the vendor wants its devices to be registered under. E.g. Verizon, GM, Ford, etc. */
  vendorId: string;
  /** Devices filter criteria or pagination token */
  filter?: DevicesRequestFilter;
}

export const devicesRequestSchema: Schema<DevicesRequest> = lazy(() =>
  object({
    vendorId: ['VendorId', string()],
    filter: ['Filter', optional(devicesRequestFilterSchema)],
  })
);

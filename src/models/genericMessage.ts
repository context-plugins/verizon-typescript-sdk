/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, object, Schema } from '../schema.js';
import { GenericPayload, genericPayloadSchema } from './genericPayload.js';

/** A message carrying a generic (custom) V2X payload. */
export interface GenericMessage {
  /**
   * Custom message which is defined by the user and can support "any" message type or format.
   * **Note:** ETX prefers the j2735 or the j2735_gr encoding and only vendor specific message types are allowed to be published in different message formats.
   */
  generic: GenericPayload;
}

export const genericMessageSchema: Schema<GenericMessage> = lazy(() =>
  object({ generic: ['generic', genericPayloadSchema] })
);

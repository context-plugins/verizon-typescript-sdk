/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { array, boolean, lazy, object, optional, Schema } from '../schema.js';
import {
  ThingspaceDevice,
  thingspaceDeviceSchema,
} from './thingspaceDevice.js';

/** Response for a request to list down account devices. */
export interface AccountDeviceListResult {
  /** Up to 10,000 devices that you want to move to a different account, specified by device identifier. */
  devices?: ThingspaceDevice[];
  /** False for a status 200 response.True for a status 202 response, indicating that there is more data to be retrieved. */
  hasMoreData?: boolean;
}

export const accountDeviceListResultSchema: Schema<AccountDeviceListResult> = lazy(
  () =>
    object({
      devices: ['devices', optional(array(thingspaceDeviceSchema))],
      hasMoreData: ['hasMoreData', optional(boolean())],
    })
);

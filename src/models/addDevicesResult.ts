/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { array, lazy, object, optional, Schema, string } from '../schema.js';
import { DeviceId, deviceIdSchema } from './deviceId.js';

/** Contains the device identifiers and a success or failure response for each device in the request. */
export interface AddDevicesResult {
  /** Identifiers for the device. */
  deviceIds?: DeviceId[];
  /** The status message for the current device. This will be Success or Failed */
  response?: string;
}

export const addDevicesResultSchema: Schema<AddDevicesResult> = lazy(() =>
  object({
    deviceIds: ['deviceIds', optional(array(deviceIdSchema))],
    response: ['response', optional(string())],
  })
);

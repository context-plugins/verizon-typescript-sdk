/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  boolean,
  lazy,
  object,
  optional,
  Schema,
  string,
} from '../schema.js';
import { CustomFields, customFieldsSchema } from './customFields.js';

/** Returns a list of all device groups in a specified account. */
export interface DeviceGroup {
  /** The description of the device group. */
  description?: string;
  /** Any extended attributes for the device group, as Key and Value pairs. */
  extendedAttributes?: CustomFields[];
  /** Identifies the default device group. */
  isDefaultGroup?: boolean;
  /** The name of the device group. */
  name?: string;
}

export const deviceGroupSchema: Schema<DeviceGroup> = lazy(() =>
  object({
    description: ['description', optional(string())],
    extendedAttributes: [
      'extendedAttributes',
      optional(array(customFieldsSchema)),
    ],
    isDefaultGroup: ['isDefaultGroup', optional(boolean())],
    name: ['name', optional(string())],
  })
);

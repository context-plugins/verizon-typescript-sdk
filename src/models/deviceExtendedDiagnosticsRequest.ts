/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { array, lazy, object, Schema, string } from '../schema.js';
import { DeviceId, deviceIdSchema } from './deviceId.js';

/** Request for obtaining device extended diagnostics. */
export interface DeviceExtendedDiagnosticsRequest {
  /** The Verizon billing account that the device belongs to. An account name is usually numeric, and must include any leading zeros. */
  accountName: string;
  /** The device for which you want diagnostic information, specified by the device's MDN. */
  deviceList: DeviceId[];
}

export const deviceExtendedDiagnosticsRequestSchema: Schema<DeviceExtendedDiagnosticsRequest> = lazy(
  () =>
    object({
      accountName: ['accountName', string()],
      deviceList: ['deviceList', array(deviceIdSchema)],
    })
);

/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  lazy,
  number,
  object,
  optional,
  Schema,
  string,
} from '../schema.js';
import {
  ETXExpectedTypeEnum,
  eTXExpectedTypeEnumSchema,
} from './eTXExpectedTypeEnum.js';
import {
  ETXMessageStandardEnum,
  eTXMessageStandardEnumSchema,
} from './eTXMessageStandardEnum.js';
import {
  RegionIntersectionPair,
  regionIntersectionPairSchema,
} from './regionIntersectionPair.js';

/** Query MAP records using specific region and intersection identifier pairs */
export interface ETXMAPMessageIntersectionCoordinates {
  /** V2X messaging standard selection. Accepted values are 'sae' (SAE J2735) and 'etsi' (ETSI TS 103 301). */
  messageStandard?: ETXMessageStandardEnum;
  /** List of region and intersection ID pairs to retrieve MAP messages for. */
  regionIntersectionPairs: RegionIntersectionPair[];
  /** The format of the payload in the response body. */
  expectedType?: ETXExpectedTypeEnum;
  /** Base64 encoded token used to retrieve the next page of results */
  pageToken?: string;
  /** Maximum number of records to return in a single page */
  pageSize?: number;
}

export const eTXMAPMessageIntersectionCoordinatesSchema: Schema<ETXMAPMessageIntersectionCoordinates> = lazy(
  () =>
    object({
      messageStandard: [
        'messageStandard',
        optional(eTXMessageStandardEnumSchema),
      ],
      regionIntersectionPairs: [
        'regionIntersectionPairs',
        array(regionIntersectionPairSchema),
      ],
      expectedType: ['expectedType', optional(eTXExpectedTypeEnumSchema)],
      pageToken: ['pageToken', optional(string())],
      pageSize: ['pageSize', optional(number())],
    })
);

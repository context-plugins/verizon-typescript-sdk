/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { array, number, object, optional, Schema, string } from '../schema.js';

/** response for api/v3/clients/connection */
export interface ConnectionResponseV3 {
  /** Array of full MQTT URLs including protocol, host, and port for each available MEC. */
  mqttURLs: string[];
  /** Array of hostnames corresponding to each MQTT URL. */
  hosts?: string[];
  /** Array of port numbers corresponding to each MQTT URL. */
  ports?: number[];
}

export const connectionResponseV3Schema: Schema<ConnectionResponseV3> = object({
  mqttURLs: ['MqttURLs', array(string())],
  hosts: ['Hosts', optional(array(string()))],
  ports: ['Ports', optional(array(number()))],
});

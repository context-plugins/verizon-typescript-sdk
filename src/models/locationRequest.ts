/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { array, lazy, object, optional, Schema, string } from '../schema.js';
import {
  AccuracyModeEnum,
  accuracyModeEnumSchema,
} from './accuracyModeEnum.js';
import { CacheModeEnum, cacheModeEnumSchema } from './cacheModeEnum.js';
import { DeviceInfo, deviceInfoSchema } from './deviceInfo.js';

/** The body contains the the account name and list of devices that you want to locate, plus other options. */
export interface LocationRequest {
  /** Account identifier in "##########-#####". */
  accountName: string;
  /** Device list. */
  deviceList: DeviceInfo[];
  /** Accurary, currently only 0-coarse supported. */
  accuracyMode?: AccuracyModeEnum;
  /** Location cache mode. */
  cacheMode?: CacheModeEnum;
}

export const locationRequestSchema: Schema<LocationRequest> = lazy(() =>
  object({
    accountName: ['accountName', string()],
    deviceList: ['deviceList', array(deviceInfoSchema)],
    accuracyMode: ['accuracyMode', optional(accuracyModeEnumSchema)],
    cacheMode: ['cacheMode', optional(cacheModeEnumSchema)],
  })
);

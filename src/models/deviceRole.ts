/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { array, number, object, optional, Schema, string } from '../schema.js';

/** The access rule (DeviceRole object) defines the topics the application or device can publish or subscribe to. It also defines how many parallel subscriptions one device or application can have and how fast it can publish messages. */
export interface DeviceRole {
  /** The unique name of the access rule. */
  name: string;
  /** The maximum number of subscriptions that one application or device can make. */
  subscribeLimit?: number;
  /** The maximum rate that one application or device can publish messages per seconds. */
  publishRateLimit?: number;
  publish?: string[];
  subscribe?: string[];
}

export const deviceRoleSchema: Schema<DeviceRole> = object({
  name: ['name', string()],
  subscribeLimit: ['subscribeLimit', optional(number())],
  publishRateLimit: ['publishRateLimit', optional(number())],
  publish: ['publish', optional(array(string()))],
  subscribe: ['subscribe', optional(array(string()))],
});

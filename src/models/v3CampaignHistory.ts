/**
 * VerizonLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  boolean,
  lazy,
  nullable,
  object,
  optional,
  Schema,
  string,
} from '../schema.js';
import {
  V3CampaignMetaInfo,
  v3CampaignMetaInfoSchema,
} from './v3CampaignMetaInfo.js';

/** Campaign history. */
export interface V3CampaignHistory {
  /** Has more report flag? */
  hasMoreData: boolean;
  /** Campaign identifier. */
  lastSeenCampaignId?: string;
  /** Firmware upgrade list. */
  campaignList: V3CampaignMetaInfo[] | null;
}

export const v3CampaignHistorySchema: Schema<V3CampaignHistory> = lazy(() =>
  object({
    hasMoreData: ['hasMoreData', boolean()],
    lastSeenCampaignId: ['lastSeenCampaignId', optional(string())],
    campaignList: ['campaignList', nullable(array(v3CampaignMetaInfoSchema))],
  })
);
